services:
  - type: web
    name: autohazard-explorer
    env: docker
    region: ohio 
    plan: free 
    branch: main
    rootDir: webdis
    dockerCommand:
    numInstances: 1
    healthCheckPath: /
